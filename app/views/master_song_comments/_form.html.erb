<%= form_for(@master_song_comment) do |f| %>
  <% if @master_song_comment.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@master_song_comment.errors.count, "error") %> prohibited this master_song_comment from being saved:</h2>

      <ul>
      <% @master_song_comment.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :user_id %><br />
    <%= f.number_field :user_id %>
  </div>
  <div class="field">
    <%= f.label :master_song_id %><br />
    <%= f.number_field :master_song_id %>
  </div>
  <div class="field">
    <%= f.label :comment %><br />
    <%= f.text_area :comment %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
